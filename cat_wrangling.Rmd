---
title: "cat wrangling"
author: "Catherine Takata"
date: "2/24/2021"
output: html_document
runtime: shiny
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(tidyverse)
library(rnaturalearth)
library(sf)
library(mapview)
```

```{r summary stats, echo = F}
foraging <- read.csv(here("data","2018_foraging.csv")) %>% 
  clean_names() %>% 
  mutate(date = mdy(date)) %>% 
  rename(aerial_survey_yr = year) %>% 
  select(c(otter_lat_deg, otter_long_deg))
```

```{r}
#dive_locations <- st_as_sf(x = foraging, 
                        #coords = c("otter_long_deg", "otter_lat_deg"),
                        #crs = "+proj=longlat +datum=WGS84 +ellps=WGS84 +towgs84=0,0,0")
#mapview(dive_locations)
```

```{r}
alaska <- ne_download(scale = 110, type = "states", category = "cultural", returnclass = "sf") %>%
  filter(name == "Alaska")
```


```{r}
tmap_mode(mode = "view")

tm_shape(alaska) +
  tm_shape(dive_locations)
```

